import { moderateScale } from 'react-native-size-matters'

export default {
  smaller: moderateScale(10),
  small: moderateScale(12),
  normal: moderateScale(14),
  header: moderateScale(16),
  pageHeader: moderateScale(18),
}
